# RandomPhrase
# Random Phrase Clicker  A small desktop app built with C# and XAML that shows a random phrase between 1 and 200 every time you press a button, while keeping track of how many times the button has been clicked.  ## Features  - Shows a random phrase (indexed from 1 to 200) on each button click   - Displays a total click counter in the UI   - Sim
